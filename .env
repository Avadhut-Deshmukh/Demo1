# Environment config for the FastAPI app
# Database (SQLite file used by the app)
DATABASE_URL=sqlite:///final_app.db

# Debug mode (True/False)
DEBUG=True

# Secret key for session/signing (change to a secure random value in production)
SECRET_KEY=change-me

# App port for running with uvicorn
PORT=8000
